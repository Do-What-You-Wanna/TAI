# AGENTS.md — 项目协作规则

> 本文件用于记录本项目的人工协作约定。AI 在项目中的所有操作都必须遵循以下规则。
> 使用者可随时编辑本文件以增删规则。

## 1. 无法解决时寻求人工协助

- 当 AI 遇到无法解决的问题（环境异常、权限不足、认证失败、逻辑冲突、结果不确定等）时，**不得擅自猜测或强行绕过**。
- 应立即明确告知使用者当前卡点、已尝试的方案、可能的原因，并请使用者协助或给出决策。

## 2. 存在多项选择时生成计划文档并征求人工

- 当一项任务存在**多种可选方案 / 方向 / 取舍**时，AI 必须先停止执行。
- 生成一份**计划文档**（Plan），清楚列出各选项的：优缺点、影响范围、推荐选择。
- 通过人工确认选定方案后，方可继续执行。
- 禁止在未征得人工选择的情况下，随意采用某一种实现。

## 3. 生成内容需含清晰注释

- 所有交付内容（代码、脚本、配置文件、文档等）必须包含**清晰、易理解的注释**。
- 注释随内容持续推进，保证使用者即使不看背景也能看懂每一步的意图。
- 关键逻辑、非显而易见的设计决策、预留的后续扩展点，均需注明。
- 注释语言与使用者沟通语言保持一致（当前为中文）。

## 4. 及时清理不需要的内容与文件

- 定期检查并清理项目中的临时文件、废弃文件、无引用代码、未使用依赖等。
- 删除前需确认其确实无用，避免误删仍在使用的内容。
- 清理后保持工作区整洁，必要时同步到版本控制。

## 5. 较大更新后自动 Git 存档并上传 GitHub

- 每次发生**较大更新**（新增功能、结构调整、大量改动等）时，自动执行：
  1. `git add` 暂存变更
  2. `git commit` 提交并存档（附清晰的中文说明）
  3. `git push` 推送到远程 GitHub 仓库，完成云备份
- 敏感文件（如 `credentials.md` 等）不得纳入提交与推送范围。
- 若推送因认证失败，请先寻求人工协助补全凭据，不得绕过安全机制。