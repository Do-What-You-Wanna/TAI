# AGENTS.md — 项目协作规则

> 本文件用于记录本项目的人工协作约定。AI 在项目中的所有操作都必须遵循以下规则。
> 使用者可随时编辑本文件以增删规则。

## 1. 无法解决时寻求人工协助

- 当 AI 遇到无法解决的问题（环境异常、权限不足、认证失败、逻辑冲突、结果不确定等）时，**不得擅自猜测或强行绕过**。
- 应立即明确告知使用者当前卡点、已尝试的方案、可能的原因，并请使用者协助或给出决策。

## 2. 存在多项选择时生成计划文档并征求人工

- 当一项任务存在**多种可选方案 / 方向 / 取舍**时，AI 必须先停止执行。
- 生成一份**计划文档**（Plan），清楚列出各选项的：优缺点、影响范围、推荐选择。
- 通过人工确认选定方案后，方可继续执行。
- 禁止在未征得人工选择的情况下，随意采用某一种实现。

## 3. 生成内容需含清晰注释

- 所有交付内容（代码、脚本、配置文件、文档等）必须包含**清晰、易理解的注释**。
- 注释随内容持续推进，保证使用者即使不看背景也能看懂每一步的意图。
- 关键逻辑、非显而易见的设计决策、预留的后续扩展点，均需注明。
- 注释语言与使用者沟通语言保持一致（当前为中文）。

## 4. 及时清理不需要的内容与文件

- 定期检查并清理项目中的临时文件、废弃文件、无引用代码、未使用依赖等。
- 删除前需确认其确实无用，避免误删仍在使用的内容。
- 清理后保持工作区整洁，必要时同步到版本控制。

## 5. 较大更新后自动 Git 存档并上传 GitHub

- 每次发生**较大更新**（新增功能、结构调整、大量改动等）时，自动执行：
  1. `git add` 暂存变更
  2. `git commit` 提交并存档（附清晰的中文说明）
  3. `git push` 推送到远程 GitHub 仓库，完成云备份
- 敏感文件（如 `credentials.md` 等）不得纳入提交与推送范围。
- 若推送因认证失败，请先寻求人工协助补全凭据，不得绕过安全机制。

## 6. 推送至 GitHub 前进行安全审查

- **每次准备 `git push` 到 GitHub 前，必须先执行敏感信息审查**，确认不推送任何敏感内容。
- 审查范围包括：密钥 / Token / 密码 / 连接串 / 个人隐私 / 内部地址与端口 / 非公开业务数据等。
- 审查方式：
  1. 检查 `git status` 与 `git diff`，确认本次将推送的变更内容。
  2. 核对是否命中 `.gitignore` 中的敏感规则（如 `credentials.md`）。
  3. 对改动内容做逐项排查，确认不含明文凭据或私密数据。
- 如发现拟推送内容中含有敏感信息：**立即终止推送**，移除敏感内容或将其加入 `.gitignore`。
- 只有确认无可疑情况后，方允许执行 `git push`。

## 7. 任务完成后自我审查并生成审查报告

- **每次任务完成后，AI 必须对本次产出做自我审查**，并按需改进项目。
- 审查要点：完成度是否符合需求、是否存在缺陷 / 潜在问题 / 未处理项、是否有更优方案。
- 每份审查产出应生成一份**审查报告**并保存到项目内（建议目录 `docs/reviews/`）：
  - 文件命名：`YYYYMMDD-HHMM-<简述>.md`。
  - 报告内容：
    1. 本次任务概述；
    2. 完成情况与交付物；
    3. 发现的问题 / 不足 / 风险；
    4. 改进建议与下一步行动。
- AI 应**根据审查报告对项目执行相应更改 / 修复**，确保问题落地解决而非仅列在报告里。
- 审查后若产生代码 / 文档变更，按规则 5 进行 Git 提交与推送（含规则 6 安全审查）。
- 若审查发现需要人工决策的事项，按规则 2 生成计划并征求人工选择。